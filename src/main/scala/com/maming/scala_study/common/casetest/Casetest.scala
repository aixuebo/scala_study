package com.maming.scala_study.common.casetest

/**
 * Created by maming on 2016/7/17.
 */
class Casetest {

  def casetest()={
 /*

    var subscriptionPattern: String = null //white_list、black_list、static之一
    var topMap: Map[String, Int] = null // {"${topic}":2,"${topic}":2} key是topic,value是该topic在group中要有多少个线程去读取

  //将topicCountString转换成jspn
  Json.parseFull(topicCountString) match {//解析zookeeper的内容

      case Some(m) =>//对json内容进行解析
        val consumerRegistrationMap = m.asInstanceOf[Map[String, Any]]//json内容是一个map对象
        consumerRegistrationMap.get("pattern") match {//获取map的key是pattern的key
          case Some(pattern) => subscriptionPattern = pattern.asInstanceOf[String]//如果有值,把String值赋值给变量subscriptionPattern
          case None => throw new KafkaException("error constructing TopicCount : " + topicCountString)
        }
        consumerRegistrationMap.get("subscription") match {//获取subscription,他是一个map对象
          case Some(sub) => topMap = sub.asInstanceOf[Map[String, Int]]//把值赋值给一个变量Map
          case None => throw new KafkaException("error constructing TopicCount : " + topicCountString)
        }
      case None => throw new KafkaException("error constructing TopicCount : " + topicCountString)
    }*/
  }
}
